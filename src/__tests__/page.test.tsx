import { expect, test } from "vitest";
import { render, screen, RenderResult } from "@testing-library/react";

test("Page", async () => {


  // expect(screen.findByText("Aguidom")).toBeDefined();
});
